apiVersion: v1
kind: Service
metadata:
  name: report-failure-prediction
  namespace: default
  labels:
    app: report-failure-prediction
spec:
  type: ClusterIP
  selector:
    app: report-failure-prediction
  ports:
  - port: 80
    targetPort: 9696
    protocol: TCP
    name: http
